[package]
name = "spice_rawfile"
version = "0.0.1"
edition = "2021"
license = "MIT"
description = "Parse SPICE rawfiles"

[dependencies]
nom = { workspace = true }
serde = { workspace = true, features = ["derive"] }
thiserror = { workspace = true }
